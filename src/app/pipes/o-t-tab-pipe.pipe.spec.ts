import { OTTabPipePipe } from './o-t-tab-pipe.pipe';

describe('OTTabPipePipe', () => {
  it('create an instance', () => {
    const pipe = new OTTabPipePipe();
    expect(pipe).toBeTruthy();
  });
});
